package braindraft.window.shapes;

import braindraft.model.network.Outputable;
import javafx.scene.shape.Shape;

/**
 *
 * @author flo
 */
public class GraphicalOutput extends GraphicalElement {

    public GraphicalOutput(final Shape shape, final Outputable outputable) {
        super(shape, outputable);
    }
}
